# Optional analysis dependencies (local / reports). Not installed in training containers by default.
# Usage from repo root: pip install -r ml/requirements.txt -r ml/requirements-analysis.txt
matplotlib==3.7.5
