AWSTemplateFormatVersion: '2010-09-09'
Description: PoEconomy training infrastructure (ECS Fargate Spot) - reads from base data lake, writes to models bucket

Parameters:
  EnvironmentName:
    Type: String
    Default: production
    AllowedPattern: '[a-zA-Z0-9-]+'
    Description: Environment identifier (e.g. dev, staging, production)
  TaskCpu:
    Type: Number
    Default: 16384
    Description: CPU units for the training task (1024 = 1 vCPU)
  TaskMemory:
    Type: Number
    Default: 65536
    Description: Memory in MB for the training task
  EphemeralStorageGiB:
    Type: Number
    Default: 100
    Description: Ephemeral storage in GiB for the training task (20-200 GiB)
  MinRecordsThreshold:
    Type: Number
    Default: 50
    Description: Minimum number of records required for training
  MaxCurrenciesToTrain:
    Type: Number
    Default: 0
    Description: Maximum number of currencies to train (0 = no limit)
  MaxCurrencyWorkers:
    Type: Number
    Default: 16
    Description: Number of parallel currency workers for training
  MaxOptunaWorkers:
    Type: Number
    Default: 2
    Description: Number of Optuna workers per currency for hyperparameter search
  ModelNJobs:
    Type: Number
    Default: 2
    Description: Number of parallel jobs for RandomForest/ExtraTrees model training
  NHyperparameterTrials:
    Type: Number
    Default: 100
    Description: Number of hyperparameter optimization trials
  NModelTrials:
    Type: Number
    Default: 500
    Description: Number of model training iterations (boosting rounds for LightGBM ceiling; n_estimators fallback for RF/ET when HPO is disabled or fails)
  CVFolds:
    Type: Number
    Default: 5
    Description: Number of cross-validation folds
  EarlyStoppingRounds:
    Type: Number
    Default: 50
    Description: LightGBM patience rounds before early stopping triggers
  EnsembleWeightOptTrials:
    Type: Number
    Default: 50
    Description: Number of Optuna trials used to optimize ensemble model weights per currency
  ValidationMaxDays:
    Type: Number
    Default: 60
    Description: Maximum days of recent DynamoDB price data fetched as the validation set
  MinAvgValueThreshold:
    Type: String
    Default: '0.25'
    Description: Minimum average value threshold for currency selection
  BaseStackName:
    Type: String
    Description: Name of the base infrastructure stack (e.g. poeconomy-production-base; pass from deploy script)
  IngestionStackName:
    Type: String
    Description: Name of the ingestion stack to import DynamoDB tables from
  DataLakeBucketName:
    Type: String
    Description: Name of the data lake bucket from base stack
  TrainingImageUri:
    Type: String
    Description: Full ECR image URI for training (including tag)
  LogLevel:
    Type: String
    Default: INFO
    AllowedValues: [DEBUG, INFO, WARNING, ERROR]
    Description: Logging level (DEBUG, INFO, WARNING, ERROR)

Resources:
  # VPC for training tasks
  TrainingVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.0.0.0/16
      EnableDnsHostnames: true
      EnableDnsSupport: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-vpc'
        - Key: Environment
          Value: !Ref EnvironmentName

  # Internet Gateway
  TrainingInternetGateway:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-igw'
        - Key: Environment
          Value: !Ref EnvironmentName

  # Attach Internet Gateway to VPC
  TrainingInternetGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      InternetGatewayId: !Ref TrainingInternetGateway
      VpcId: !Ref TrainingVPC

  # Public Subnet A
  TrainingPublicSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref TrainingVPC
      AvailabilityZone: !Select [0, !GetAZs '']
      CidrBlock: 10.0.1.0/24
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-public-subnet-a'
        - Key: Environment
          Value: !Ref EnvironmentName

  # Public Subnet B
  TrainingPublicSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref TrainingVPC
      AvailabilityZone: !Select [1, !GetAZs '']
      CidrBlock: 10.0.2.0/24
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-public-subnet-b'
        - Key: Environment
          Value: !Ref EnvironmentName

  # Route Table
  TrainingPublicRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref TrainingVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-public-routes'
        - Key: Environment
          Value: !Ref EnvironmentName

  # Default Route
  TrainingDefaultPublicRoute:
    Type: AWS::EC2::Route
    DependsOn: TrainingInternetGatewayAttachment
    Properties:
      RouteTableId: !Ref TrainingPublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref TrainingInternetGateway

  # Associate Subnet A with Route Table
  TrainingPublicSubnetARouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref TrainingPublicRouteTable
      SubnetId: !Ref TrainingPublicSubnetA

  # Associate Subnet B with Route Table
  TrainingPublicSubnetBRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref TrainingPublicRouteTable
      SubnetId: !Ref TrainingPublicSubnetB

  # Security Group
  TrainingSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupName: !Sub '${AWS::StackName}-sg'
      GroupDescription: Security group for PoEconomy training tasks
      VpcId: !Ref TrainingVPC
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-sg'
        - Key: Environment
          Value: !Ref EnvironmentName

  # ECS Cluster
  TrainingCluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Sub '${AWS::StackName}'
      CapacityProviders:
        - FARGATE_SPOT
      DefaultCapacityProviderStrategy:
        - CapacityProvider: FARGATE_SPOT
          Weight: 1
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-cluster'
        - Key: Environment
          Value: !Ref EnvironmentName

  # CloudWatch Log Group
  TrainingLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/ecs/${AWS::StackName}'
      RetentionInDays: 14
      Tags:
        - Key: Environment
          Value: !Ref EnvironmentName
        - Key: App
          Value: PoEconomy
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete

  # Task Definition
  TrainingTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}'
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref TaskCpu
      Memory: !Ref TaskMemory
      EphemeralStorage:
        SizeInGiB: !Ref EphemeralStorageGiB
      ExecutionRoleArn: !GetAtt TrainingExecutionRole.Arn
      TaskRoleArn: !GetAtt TrainingTaskRole.Arn
      ContainerDefinitions:
        - Name: training
          Image: !Ref TrainingImageUri
          Essential: true
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref TrainingLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs
          Environment:
            - Name: ENVIRONMENT
              Value: !Ref EnvironmentName
            - Name: MIN_RECORDS_THRESHOLD
              Value: !Ref MinRecordsThreshold
            - Name: MAX_CURRENCIES_TO_TRAIN
              Value: !Ref MaxCurrenciesToTrain
            - Name: DATA_LAKE_BUCKET
              Value: !Ref DataLakeBucketName
            - Name: MODELS_BUCKET
              Value: !Ref DataLakeBucketName
            # Threading and Parallelization
            - Name: MAX_CURRENCY_WORKERS
              Value: !Ref MaxCurrencyWorkers
            - Name: MAX_OPTUNA_WORKERS
              Value: !Ref MaxOptunaWorkers
            - Name: MODEL_N_JOBS
              Value: !Ref ModelNJobs
            # Logging
            - Name: LOG_LEVEL
              Value: !Ref LogLevel
            - Name: DISABLE_FILE_LOGGING
              Value: 'true'
            # Hyperparameter Optimization
            - Name: N_HYPERPARAMETER_TRIALS
              Value: !Ref NHyperparameterTrials
            - Name: N_MODEL_TRIALS
              Value: !Ref NModelTrials
            - Name: CV_FOLDS
              Value: !Ref CVFolds
            - Name: EARLY_STOPPING_ROUNDS
              Value: !Ref EarlyStoppingRounds
            - Name: ENSEMBLE_WEIGHT_OPT_TRIALS
              Value: !Ref EnsembleWeightOptTrials
            # Data Selection
            - Name: MIN_AVG_VALUE_THRESHOLD
              Value: !Ref MinAvgValueThreshold
            - Name: VALIDATION_MAX_DAYS
              Value: !Ref ValidationMaxDays
            # DynamoDB Tables
            - Name: DYNAMO_LEAGUE_METADATA_TABLE
              Value:
                Fn::ImportValue:
                  Fn::Sub: '${IngestionStackName}-LeagueMetadataTable'
            - Name: DYNAMO_DAILY_PRICES_TABLE
              Value:
                Fn::ImportValue:
                  Fn::Sub: '${IngestionStackName}-DailyPricesTable'
          StopTimeout: 60
      Tags:
        - Key: Environment
          Value: !Ref EnvironmentName
        - Key: App
          Value: PoEconomy

  # IAM Role for ECS Task Execution
  TrainingExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-execution-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-execution-role'
        - Key: Environment
          Value: !Ref EnvironmentName

  # IAM Role for ECS Task
  TrainingTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-task-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: DataLakeAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:DeleteObject
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::${DataLakeBucketName}'
                  - !Sub 'arn:aws:s3:::${DataLakeBucketName}/*'
        - PolicyName: CloudWatchLogs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                  - logs:DescribeLogStreams
                Resource: '*'
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:DescribeTable
                  - dynamodb:Query
                  - dynamodb:Scan
                Resource:
                  - !Sub 
                    - 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${LeagueMetadataTable}'
                    - LeagueMetadataTable: 
                        Fn::ImportValue:
                          Fn::Sub: '${IngestionStackName}-LeagueMetadataTable'
                  - !Sub 
                    - 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${DailyPricesTable}'
                    - DailyPricesTable: 
                        Fn::ImportValue:
                          Fn::Sub: '${IngestionStackName}-DailyPricesTable'
                  - !Sub 
                    - 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${DailyPricesTable}/index/league-date-index'
                    - DailyPricesTable: 
                        Fn::ImportValue:
                          Fn::Sub: '${IngestionStackName}-DailyPricesTable'
                  - !Sub 
                    - 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${DailyPricesTable}/index/currency-date-index'
                    - DailyPricesTable: 
                        Fn::ImportValue:
                          Fn::Sub: '${IngestionStackName}-DailyPricesTable'
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-task-role'
        - Key: Environment
          Value: !Ref EnvironmentName

Outputs:
  TrainingClusterName:
    Description: ECS cluster name for training tasks
    Value: !Ref TrainingCluster
    Export:
      Name: !Sub '${AWS::StackName}-TrainingClusterName'

  TrainingTaskDefinitionArn:
    Description: ECS task definition ARN for training tasks
    Value: !Ref TrainingTaskDefinition
    Export:
      Name: !Sub '${AWS::StackName}-TrainingTaskDefinitionArn'

  TrainingVpcId:
    Description: VPC ID for training infrastructure
    Value: !Ref TrainingVPC
    Export:
      Name: !Sub '${AWS::StackName}-TrainingVpcId'

  TrainingSecurityGroupId:
    Description: Security group ID for training tasks
    Value: !Ref TrainingSecurityGroup
    Export:
      Name: !Sub '${AWS::StackName}-TrainingSecurityGroupId'

  TrainingPublicSubnetAId:
    Description: Public subnet A in the training VPC (same VPC as TrainingSecurityGroupId)
    Value: !Ref TrainingPublicSubnetA
    Export:
      Name: !Sub '${AWS::StackName}-TrainingPublicSubnetAId'

  TrainingPublicSubnetBId:
    Description: Public subnet B in the training VPC (same VPC as TrainingSecurityGroupId)
    Value: !Ref TrainingPublicSubnetB
    Export:
      Name: !Sub '${AWS::StackName}-TrainingPublicSubnetBId'
